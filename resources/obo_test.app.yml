resources:
  apps:
    obo_test:
      name: obo-test-${bundle.target}
      description: "OBO authentication test app - browse Unity Catalog as the logged-in user"
      source_code_path: ../src/app

      user_api_scopes:
        - sql

      permissions:
        - level: CAN_USE
          group_name: "users"
